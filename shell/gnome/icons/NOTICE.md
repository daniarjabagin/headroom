# Provider logos

The provider logos are symbolic copies (single path, fill `#bebebe` so GNOME recolors them with the
text color) of the [Simple Icons](https://simpleicons.org) originals in `assets/providers/`
(CC0-1.0). `claude.svg` keeps the brand color `#D97757`. See `assets/providers/NOTICE.md` for sources
and trademarks; Headroom is not affiliated with or endorsed by any of the providers. Files are named
after the provider id, so `moonshot-symbolic.svg` is the `moonshotai` logo, `zai-symbolic.svg` the
`zdotai` one and `copilot-symbolic.svg` the `githubcopilot` one. Grok, Devin and Kilo Code have no
Simple Icons logo and use `provider-symbolic.svg`.

Lookup by provider id: `<id>.svg` (full color) first, then `<id>-symbolic.svg` (follows the text
color), else `provider-symbolic.svg`, the generic icon drawn for Headroom.

`headroom-symbolic.svg` is the monochrome Headroom mark from the Asteru Studio brandbook
(© Asteru Studio / Daniar Jabagin, all rights reserved; not covered by the MIT license, see
`assets/brand/README.md`). It is a single `#bebebe` path, so the panel recolors it like any symbolic
icon.
