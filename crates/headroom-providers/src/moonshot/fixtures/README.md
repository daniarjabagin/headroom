# Moonshot fixtures

Synthetic responses in the shape documented at <https://platform.kimi.ai/docs/api/balance> (USD,
`api.moonshot.ai`) and <https://platform.kimi.com/docs/api/balance> (CNY, `api.moonshot.cn`).
`balance.json` is the documented example. No real account was queried; replace them with
anonymised real responses when one is available.
