# DeepSeek fixtures

Synthetic responses in the shape documented at
<https://api-docs.deepseek.com/api/get-user-balance> (`balance.json` is the documented example).
No real account was queried; replace them with anonymised real responses when one is available.
